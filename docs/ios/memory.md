# iOS working memory

- Active slice: S03 — Leaderboard
- Active branch: `feature/ios-leaderboard`
- Base: refreshed `origin/master` at merge commit `8f106168fc08f3e9d76a0681e9b19ef5b95923da` (PR [#101](https://github.com/sriram294/sports-leaderboard/pull/101), merged 2026-08-31)
- Current state: S02 is merged and closed, and the Board placeholder is replaced by S03. The injected leaderboard repository preserves the existing response and month-window contract with one auth-refresh retry. Immutable state covers month/all-time ranges, canonical and stable metric sorting, provisional partitioning, cancellation, loading/empty/failure/stale refresh behavior, and injected clock/calendar boundaries. The SwiftUI board includes month recognition, a three-lane podium, complete ranking rows, photo/avatar/initial fallback, form indicators, metric cycling, pull-to-refresh, accessibility summaries and stacked Dynamic Type alternatives. Deterministic UI scenarios attach podium, dense accessibility, empty, failure, and light/dark screenshots to the future `.xcresult`.
- Blockers: Xcode 26.2 is unavailable in this Linux workspace, so native build, UI-test, archive, and screenshot evidence must be captured in Codemagic or on a pinned macOS environment. S02 merged without a backend self-leave contract; that debt remains recorded but does not require expanding S03's leaderboard-only API scope.
- Known-good behavior: S00/S01 remain intact and PR #101 merged S02 with both Vercel checks passing. On 2026-08-31, the full iOS Swift tree parsed under Swift 6.2, the group + leaderboard non-UI sources and tests passed Swift 6 strict-concurrency type-checking with warnings as errors, and an isolated `swift:6.2-jammy` run passed 12/12 deterministic tests across four suites. That run also found and fixed S02's fire-and-forget first-group selection persistence race. `ios/scripts/verify-repository.sh`, documentation/secret checks, shell parsing, and `git diff --check` pass. No Xcode artifacts exist yet.
- Verification commands: `cd ios && ./scripts/resolve-packages.sh && ./scripts/build.sh && ./scripts/test.sh && ./scripts/verify-repository.sh`; `cd backend && ./mvnw test`
- Exact next action: open the S03 PR so the generalized Codemagic workflow can run on Xcode 26.2; fix any SwiftUI compile or UI-test failures, then retain `.xcresult`, JUnit, unsigned archive, and the attached month/all-time, dense, empty, failure, light, and dark screenshots.

Do not start match, profile/stat, notification, or account-lifecycle work from later slices. Do not mark S03 `done` until S04's first transition commit after the S03 PR merges.
