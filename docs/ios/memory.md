# iOS working memory

- Active slice: S02 — Groups and app shell
- Active branch: `feature/ios-groups-shell`
- Base: refreshed `origin/master` at merge commit `490fecba985f48d10c6ffc72b3d2014390f28c3f` (PR [#100](https://github.com/sriram294/sports-leaderboard/pull/100), merged 2026-08-31)
- Current state: Authenticated users route into the native five-tab shell. S02 now has an injected live group repository with one-time auth refresh/retry, UserDefaults-backed selected-group persistence, stale-selection fallback, foreground resync, create/join/switch/rename/invite/session-time flows, member addition/removal, owner-only role changes, share-sheet invite copy, loading/empty/failure/permission states, destructive confirmation, and deterministic repository/view-model/UI scenarios. Full Xcode execution and the leave-group decision remain outstanding.
- Blockers: the S02 outcome requires leaving groups, but the current backend exposes no leave endpoint and `DELETE /groups/{groupId}/members/{self}` explicitly returns `GROUP_CANNOT_REMOVE_SELF`; implementing leave requires an agreed backend contract change despite S02's “no planned endpoint changes” constraint. Xcode 26.2 is unavailable in this Linux workspace, so native build, UI-test, archive, and screenshot evidence must be captured in Codemagic or on a pinned macOS environment. Real auth provider identifiers remain external configuration and do not block deterministic S02 work.
- Known-good behavior: S00/S01 behavior remains intact. On 2026-08-31, the backend suite passed 147/147 tests on JDK 25 against PostgreSQL 16 with Flyway V1–V15 and PR #100's two Vercel checks passed. For S02, `ios/scripts/verify-repository.sh`, documentation/secret checks, and shell parsing pass; the full iOS source and test tree parses under Swift 6.2 in `swift:6.2-jammy`, and the non-UI group repository/state/view-model plus unit-test sources pass Swift 6 strict-concurrency type-checking with warnings as errors. No Xcode artifacts exist yet.
- Verification commands: `cd ios && ./scripts/resolve-packages.sh && ./scripts/build.sh && ./scripts/test.sh && ./scripts/verify-repository.sh`; `cd backend && ./mvnw test`
- Exact next action: decide whether S02 may add a self-leave backend endpoint (including owner handling); then implement that decision and run `resolve-packages.sh`, `build.sh`, `test.sh`, and `archive-unsigned.sh` on Xcode 26.2, capturing `.xcresult`, JUnit, archive, and light/dark role/state screenshots.

Do not start leaderboard, match, profile/stat, notification, or account-lifecycle work from later slices. Do not mark S02 `done` until S03's first transition commit after the S02 PR merges.
