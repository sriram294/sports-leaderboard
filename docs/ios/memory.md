# iOS working memory

- Active slice: S07 — Account lifecycle and hardening
- Active branch: `feature/ios-account-lifecycle`
- Base: refreshed `origin/master` at `a3ddb7b17cab788895cb68d865495e4e2ef7fd7e` after S06 PR [#107](https://github.com/sriram294/sports-leaderboard/pull/107) merged; S05 previously merged as PR [#106](https://github.com/sriram294/sports-leaderboard/pull/106) at `317dbff99d45234d3417b92ecc17407858cacf3e`
- Current state: S04 is merged and closed by PR #105 (merge SHA unavailable in this workspace). S05 is merged and closed by PR #106 at `317dbff99d45234d3417b92ecc17407858cacf3e`. S06 is merged and closed by PR #107 at `a3ddb7b17cab788895cb68d865495e4e2ef7fd7e`. S07 is now active for account deletion, credential revocation, history unlinking/anonymization, local data purge, and privacy/security hardening.
- Blockers: Xcode 26.2 and `swiftc` are unavailable in this Linux workspace, so new Swift sources have not yet been compiled or executed here. The backend integration suite cannot connect to PostgreSQL in this workspace; `MatchServiceIntegrationTest` therefore stops during application-context startup before exercising V16. The generalized Codemagic workflow should provide macOS evidence on the S04 PR. S02's backend self-leave debt remains outside S04 scope.
- Known-good behavior: S00–S03 remain merged. For S04, `cd backend && ./mvnw -q -DskipTests compile` passes on JDK 25, `cd ios && ./scripts/verify-repository.sh` passes, and `git diff --check` is clean. The attempted targeted backend suite reached Spring startup but failed only because PostgreSQL was unavailable; the attempted iOS resolve/build/test commands stop at the recorded missing-`xcodebuild` blocker. No successful S04 Xcode or database-backed test artifact has been reported.
- Verification commands: `cd ios && ./scripts/resolve-packages.sh && ./scripts/build.sh && ./scripts/test.sh && ./scripts/verify-repository.sh`; `cd backend && ./mvnw test`
- Exact next action: inspect the existing account-deletion backend contract and data relationships, then implement the iOS account repository, re-authentication and provider-revocation adapters, destructive confirmation flow, local credential/data purge, tests, and UI scenarios; run the iOS/backend verification commands with Xcode 26.2 and record `.xcresult`, JUnit, database/API assertions, deletion screenshots, and privacy/secret scan artifact paths.

Do not start release-readiness work from later slices. Do not mark S07 `done` until S08's first transition commit after the S07 PR merges.
