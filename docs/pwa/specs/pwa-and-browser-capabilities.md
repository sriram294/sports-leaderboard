# PWA and browser capabilities

The service worker caches the app shell and lets API requests remain network-first. Share, clipboard, file input, notifications, and push are progressive enhancements. No capability failure blocks recording matches or reading already-loaded data. Deployments must support SPA fallback and version cache names when shell assets change.
