package com.org.playboard.ui.theme

import androidx.compose.ui.graphics.Color

// Brand accent — same hex as the backend's AvatarColorPicker palette
// (com.org.playboard.common.AvatarColorPicker), an intentional tie-in.
val BrandLime = Color(0xFF9ADE28)
val OnBrandLime = Color(0xFF0A0A0A)

// Dark surfaces — the app's theme is dark-only (see login.pdf), not adaptive.
val BackgroundDark = Color(0xFF0A0A0A)
val SurfaceDark = Color(0xFF141414)

// Text
val TextPrimary = Color(0xFFF5F5F5)
val TextMuted = Color(0xFF9E9E9E)

// Stat accents (leaderboard.pdf) — wins/losses columns and win-rate tiers.
val StatWinGreen = Color(0xFF4ADE80)
val StatLossRed = Color(0xFFF87171)
val WinRateMidAmber = Color(0xFFFACC15)
val WinRateLowBlue = Color(0xFF60A5FA)
